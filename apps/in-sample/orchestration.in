enable distributed-workers;
enable gpu-optimizer;

@gpu
distributed fn process-video(video: Video) -> void {
  return;
}

@parallel-safe
fn warm-cache() -> void {
  return
}

parallel {
  warm-cache();
}

struct Video { Int id }

fn main() -> void {
  warm-cache()
  return
}
