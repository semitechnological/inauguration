package ecosystem-demo;
module ecosystem-demo.main;
use cargo:crepuscularity;
use npm:hono;
use pip:flask;
use go:fiber;

fn main() -> String {
  return hono-greet() + ":" + flask-greet() + ":" + crepuscularity-greet() + ":" + fiber-greet();
}