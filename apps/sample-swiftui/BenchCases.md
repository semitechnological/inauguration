# Bench Cases

1. Body text edit in `ContentView.swift` (expected patch path)
2. Modifier change in `ContentView.swift` (expected patch path)
3. App root change in `SampleApp.swift` (expected restart fallback path)
