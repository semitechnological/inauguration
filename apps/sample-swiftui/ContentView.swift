import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Inauguration MVP")
                .font(.title)
            Text("Edit this file to trigger daemon patch events")
                .font(.caption)
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}
