# ocaml-front

Minimal Swift subset parser + checker.

## Supported subset

- `struct Name`
- `func name()`
- Required entry check for `func main()`

## Output

JSON lines artifact, consumed by Rust pipeline.
