// @expect parse: ok
// Status: STUB — async features are Wave 3 design.
// Parse-only fixture for parser validation.

fn slow-op() -> Int {
  return 42;
}

fn main() -> void {
  let result: Int = slow-op();
  return;
}
