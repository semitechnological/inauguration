// @expect parse: ok
// @expect has-function: countdown
// @expect bytecode: executes
// @expect result: 0

fn countdown(n: Int) -> Int {
  let c: Int = n;
  while (c > 0) {
    c = c - 1;
  }
  return c;
}

fn main() -> Int {
  return countdown(5);
}
