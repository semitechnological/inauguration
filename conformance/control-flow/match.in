// @expect parse: ok
// @expect has-function: grade
// @expect has-call-edge: grade
// bytecode result not verified: match return lowering is WIP in bytecode backend

fn grade(n: Int) -> Int {
  match n {
    1 -> { return 1; }
    2 -> { return 2; }
    _ -> { return 0; }
  }
}

fn main() -> void {
  let g: Int = grade(1);
  return;
}
