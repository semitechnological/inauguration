// @expect parse: ok
// @expect has-function: sign
// @expect bytecode: executes
// @expect result: 1

fn sign(n: Int) -> Int {
  if (n > 0) {
    return 1;
  } else {
    if (n < 0) {
      return -1;
    } else {
      return 0;
    }
  }
}

fn main() -> Int {
  return sign(42);
}
