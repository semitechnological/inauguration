// conformance/functions/params.in
// Tests: function parameters
// Expected: parse ok, graph shows params with correct types

fn add(a: Int, b: Int) -> Int {
  return a + b;
}

fn greet(name: String) -> String {
  return "Hello";
}

fn main() -> void {
  let sum: Int = add(1, 2);
  let msg: String = greet("world");
  return;
}
