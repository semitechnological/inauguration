import std.fs;
capability fs.read;
capability fs.write;

fn main() -> String {
  let ok: Bool = write-file("/tmp/in-stdlib-runtime-fixture.txt", "hello compiler");
  return read-file("/tmp/in-stdlib-runtime-fixture.txt");
}
