package inauguration.docs;
module inauguration.docs.backend;

import std.io;
import std.process;

capability process.spawn;

fn main() -> void {
  print("inauguration docs-site");
  process_run("in execute docs-site/build.in --module-id inauguration.docs.build");
  return;
}