package inauguration.docs;
module inauguration.docs.build;

import std.io;
import std.process;

capability process.spawn;

fn main() -> void {
  print("inauguration docs-site (inlang)");
  process-run("in execute docs-site/scripts/gen-splash.in --module-id inauguration.docs.gen-splash");
  process-run("bash scripts/build-docs-site.sh");
  print("ok https://inauguration.tsc.hk");
  return;
}