# Baseline (2026-05-08)

Bootstrap baseline for Week 6 tracking.

## Rust pipeline baseline

- Command: `cargo test --all`
- Result: pass
- Observed wall time: ~1.8s incremental, ~9.5s cold

## Swift preview host baseline

- Command: `swift build -Xswiftc -warnings-as-errors && swift test`
- Result: pass
- Observed build time: ~0.21s incremental build, ~5.63s test build

## OCaml front baseline

- Command: `dune runtest`
- Result: blocked (`dune` not installed in environment)

## Next metrics

- Add scripted edit scenarios in `apps/sample-swiftui/BenchCases`.
- Collect edit-to-preview p50/p95 from daemon + preview host logs.
