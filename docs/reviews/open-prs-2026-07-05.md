# Open PR review snapshot (2026-07-05)

## Security (merge first)

| PR | Verdict | Notes |
|----|---------|-------|
| [#66](https://github.com/tschk/inauguration/pull/66) command injection | **MERGE** | `Command::new(path)` not `/bin/sh -c`; test-only paths |
| [#52](https://github.com/tschk/inauguration/pull/52) UTF-8 unwrap | **HOLD** | Rebase after #66; keep `native_stdlib` + `owned_compile` hunks only |

**Master cherry-pick:** `lower_tests.rs` now matches #66 (#52 overlap).

## Perf (triage)

| PR | Verdict |
|----|---------|
| #70 struct init clone | Review diff size; merge if tests green |
| #69 collect_free_vars clone | Same |

## Hygiene / tests (batch)

PRs #41–#68 mostly unit tests + dead-code removal — merge in small batches after `in test`; low risk unless they touch emit paths (#63 `lower_call_expr`).

## Parsers / types (repo work, not PRs)

- `uses_family_typecheck` → `language_support::can_typecheck()` (merged locally).
- `normalize_entry` widened for `main` / `Main` / case-insensitive.
- `normalize_type` aliases: `long`, `char`, `byte`, `short`, `uintptr`, `size_t`, `ssize_t`.

Next: `.in` `parse_param` fail on bad types; Go/Swift gate samples after family alignment.