package capability_demo;
module capability_demo.main;
use std.io;

fn hello() -> String { return "capabilities: parse + lower + typecheck + boundary + bytecode"; }

fn main() -> void {
  print(hello());
  return;
}
