package even-odd;
module even-odd.main;

fn is-even(n: Int) -> Bool {
  if n == 0 { return true; }
  return is-odd(n - 1);
}

fn is-odd(n: Int) -> Bool {
  if n == 0 { return false; }
  return is-even(n - 1);
}

fn main() -> Bool { return is-even(42); }
