package factorial;
module factorial.main;

fn factorial(n: Int) -> Int {
  let result = 1;
  let i = 1;
  while i <= n {
    result = result * i;
    i = i + 1;
  }
  return result;
}

fn main() -> Int { return factorial(5); }
