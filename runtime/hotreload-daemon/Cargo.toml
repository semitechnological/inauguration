[package]
name = "hotreload-daemon"
version = "0.1.0"
edition = "2021"

[dependencies]
notify = "8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "fs", "net", "io-util"] }
