If the issue is "Implement real type checker" and the rationale is "Requires deep understanding of the type system and likely extensive architectural additions", this sounds like a "convert TODO to issue URL" or "convert TODO to `todo!()` macro" or something. But `todo!()` macro panics, changing behavior.

Wait. What if there's a code health issue where it says "TODO: implement real type checker" but it shouldn't be here, or we should change it to log a warning, or add a proper tracking issue link? "often requires tracking issues". Wait, what if there is an issue tracker URL? Let me look at other TODOs in the codebase.
